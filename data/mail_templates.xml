<odoo>
  <data noupdate="0">

    <record id="mail_template_fiscal_echeance_reminder" model="mail.template">
      <field name="name">Notification Échéance Fiscale</field>
      <field name="model_id" ref="model_fiscal_echeance"/>
      <field name="subject">Rappel : Échéance fiscale « ${object.name} » le ${format_date(object.deadline, lang=object.partner_id.lang)}</field>
      <field name="email_from">no-reply@cga.local</field>
      <field name="email_to">${object.partner_id.email}</field>
      <field name="auto_delete" eval="True"/>
      <field name="lang">${object.partner_id.lang}</field>
      <field name="body_html"><![CDATA[
        <p>Bonjour ${object.partner_id.name},</p>

        <p>Nous vous rappelons que vous avez une échéance fiscale à venir :</p>

        <ul>
          <li><strong>Impôt :</strong> ${object.name}</li>
          <li><strong>Régime :</strong> ${object.regime_id.name}</li>
          <li><strong>Date limite :</strong> ${format_date(object.deadline, lang=object.partner_id.lang)}</li>
        </ul>

        <p>Merci de veiller à régulariser votre situation avant cette date.</p>

        <p>Cordialement,<br/>
        Le Centre de Gestion Agréé</p>
      ]]></field>
    </record>

  </data>
</odoo>
